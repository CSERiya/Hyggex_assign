## Steps required for running this project on your system.
## Tailwind CSS

Tailwind CSS styles are already included in the project build. There's no need to install Tailwind CSS separately. 

## Installation
1. Clone the repository:
git clone <repository_url>

2. Install dependencies:
npm install

3. Run the development server:
npm start

